# Ignore intermediate files created by LaTeX
*.synctex.gz

# Ignore files generated by LaTeX compilers and editors
*.fdb_latexmk
*.fls

# If you’re using any build directories, like `build` or `out`, add them too
build/
out/







